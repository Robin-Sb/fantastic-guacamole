namespace Malefiz {
  export enum COLOR {
    RED = "R",
    GREEN = "G",
    BLUE = "B",
    YELLOW = "Y"
  }  
  export enum STAGE {
    ROLL,
    PICK_TOKEN,
    MOVE_TOKEN,
    MOVE_BARRIER
  }

  export enum TYPE {
    PLAYER_RED,
    PLAYER_GREEN,
    PLAYER_YELLOW,
    PLAYER_BLUE,
    BARRIER,
    WIN
  }

}
